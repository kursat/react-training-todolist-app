import { Button } from '@nextui-org/react';

function App() {
    return (
        <div className="App">
            <Button variant={'shadow'} color={'secondary'}>
                Test
            </Button>
        </div>
    );
}

export default App;
